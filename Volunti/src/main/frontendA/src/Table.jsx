"use strict";
const React = require('react');

var Table = React.createClass({
    render: function () {
        return(
            <div>
                this.props.data
            </div>
        )
    }
})

module.exports = Table;